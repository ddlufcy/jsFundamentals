function zach(ticket) {
    console.log(`I exchanged my ticket for a ${ticket}`);
}

zach('cheese pizza');

//function parameters are placeholders for data that we pass into the function when calling ot 'invoking' the function

//functions need parameters to accept data that we give to them

function fullName(firstName, lastName){
    console.log(`Hello, my name is ${firstName} ${lastName}`)
}

fullName('Daniel', 'Lufcy');